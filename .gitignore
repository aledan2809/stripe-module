node_modules/
dist/
.env
.env.local
.env.production
.credentials.json
data/credentials.json
admin/.next/
admin/node_modules/

# Next.js auto-generated typegen
**/next-env.d.ts

# npm pack artifact
projects-stripe-module-*.tgz

# broker runtime session store (contains callbackSecret)
data/checkout-sessions.json

# runtime data (edited via admin UI / API — NOT seed; gitignored so deploys don't clobber live mappings/companies)
data/project-mappings.json
data/companies.json

# invoice email: SMTP config (encrypted password) + send log (PII: customer emails)
data/email-config.json
data/invoice-emails.json
